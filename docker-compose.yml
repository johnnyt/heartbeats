services:
  postgres:
    image: postgres:16
    container_name: heartbeats-pg
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - heartbeats-pg-data:/var/lib/postgresql/data

volumes:
  heartbeats-pg-data:
